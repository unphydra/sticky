import React from 'react';
import Posts from './Posts';

const Container = (props) => (
  <div style={{ width: '600px', margin: 'auto', paddingTop: '100px' }}>
    <Posts></Posts>
  </div>
);

export default Container;
